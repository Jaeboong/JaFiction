# Hand-off: Realtime Orchestration Overhaul

> 작성: 2026-04-09  
> 전체 설계 원본: `/home/cbkjh0225/.claude/plans/resilient-sniffing-rose.md`

---

## 현재 상태 요약

### Part A — Intervention 즉시 반응 시스템 (⚠️ 미완)

Codex가 구현을 진행했으나 테스트 fix 단계에서 중단됨.

**구현 완료된 것**:
- `RunInterventionAbortError` 클래스 추가 (`packages/shared/src/core/types.ts` 또는 `providers.ts`)
- `runSessionManager.ts`: `executionAbortController` 필드, `bindExecutionAbortController()` 메서드, `submitIntervention()` → realtime 모드에서 즉시 `abort(new RunInterventionAbortError(...))` 호출
- `orchestrator.ts`: `createLinkedAbortController()` 유틸, `bindExecutionAbortController` 콜백 전파, `handleImmediateRealtimeIntervention()` 핸들러, `buildInterventionCoordinatorPrompt()` 프롬프트 빌더

**미완인 것**:
- `orchestrator.test.ts`의 `force-close` 시나리오 테스트 실패 중 — 개입 후 handoff는 동작하나 종료 조건 불일치로 루프가 늘어짐
- `check.sh` 최종 통과 미확인
- `apply-dev-stack` 미실행

**다음 세션이 할 일 (Part A 마무리)**:
1. `force-close` 테스트 실패 원인 파악 (`orchestrator.test.ts` 해당 케이스)
2. 종료 조건 수정 후 `check.sh` 통과
3. `apply-dev-stack` 실행해 스택 반영 확인

---

### Part B — 수렴·품질 개선 (미시작)

Part A `check.sh` 통과 후 Codex에 위임. 전체 설계는 원본 플랜 참조.

구현 순서 (플랜 그대로):
1. `SectionOutcome` 타입에 `"deferred-close"` 추가 (`types.ts:74`, `schemas.ts` Zod enum, `orchestrator.ts:2883` validateSectionOutcome)
2. 챌린지 티켓 ID 불투명화 (`orchestrator.ts:2660` `buildChallengeTicketId` → sha1 해시 6자리)
3. 존댓말 규칙 블록 주입 (4개 프롬프트 빌더: L1869, L1947, L2027, L2328)
4. Section Role Boundary 블록 (`buildRealtimeCoordinatorDiscussionPrompt` L1869, `buildRealtimeCoordinatorRedirectPrompt` L1947)
5. Reviewer 다수결 + Convergence Pressure (realtime 루프 ~L877 집계 로직, Coordinator 프롬프트 주입)

---

## 핵심 파일 위치

| 파일 | 내용 |
|------|------|
| `packages/shared/src/core/orchestrator.ts` | 메인 로직 |
| `packages/shared/src/controller/runSessionManager.ts` | abort 트리거 |
| `packages/shared/src/core/types.ts` | `SectionOutcome`, `RunInterventionAbortError` |
| `packages/shared/src/core/schemas.ts` | Zod enum |
| `packages/shared/src/test/orchestrator.test.ts` | 테스트 (force-close 케이스 실패 중) |

## 검증 명령

```bash
./scripts/check.sh
./scripts/apply-dev-stack.sh
```

## Claude 역할 원칙

- Claude = Head Agent (판단·총괄·커뮤니케이션만)
- 코드 분석/수정 = 반드시 Codex에 위임
- Codex에게는 설계를 Claude가 먼저 완성한 뒤 구현만 넘길 것 (설계를 Codex에 맡기지 말 것)
