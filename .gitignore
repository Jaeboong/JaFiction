node_modules/
dist/
dist-bin/
coverage/
.DS_Store
.forjob/
.jasojeon/
.worktrees/
worktrees/
.harness/
dist-tools/
*.tsbuildinfo
*.bun-build

# 공고 파서 fixture HTML (원본만, 커밋 방지)
docs/plans/2026-04-17-posting-parser-fixtures/fetched/

# DART 사업보고서 fixture 원본 (커밋 방지 — 2026-07-04-dart-filings-rag P0)
services/filings-retrieval/eval/fixtures/raw/

# Python (services/**)
__pycache__/
*.pyc
.venv/
.pytest_cache/

# Claude Code local state (never commit)
.claude/

# nginx config (server-managed)
nginx/

# Secrets — never commit
.env
.env.*
!.env.example
!.env.production.example
!.env.dev.example
client_secret*.json
*.pem
*.key

# 로컬 테스트 산출물 (자소서 샘플·채점 결과 — 커밋하지 않음)
docs/test_output/
